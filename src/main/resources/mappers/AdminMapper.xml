<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.AdminMapper">


<!--登入-->
    <select id="getAdminByAccountPassword" resultType="com.example.demo.entity.Admin">
        SELECT id,account,password,name,phone,email,is_admin,created_time,updated_time FROM admin WHERE account = #{account} AND password = #{password}
    </select>

</mapper>